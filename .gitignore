# OS
.DS_Store

# Editor / harness
.claude/

# Raw source videos — too big for GitHub (100MB limit).
# Transcode these into hero-bg.mp4 with ffmpeg, commit only the small version.
SizzleReel_*.mp4
animationReel_*.mp4

# Resume — held out of the repo until phone number is scrubbed.
resume-*.pdf
